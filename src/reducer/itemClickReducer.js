import * as actionTypes from '../const/actionTypes';
const initalState = {};
const itemClickReducer = (state = initalState, action) => {
  switch(action.type){
    default: return state
  }
}
export default itemClickReducer;